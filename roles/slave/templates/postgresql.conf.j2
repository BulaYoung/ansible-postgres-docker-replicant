# PostgreSQL configuration file

# Connection settings
listen_addresses = '*'
port = 5432

# Replication settings
wal_level = replica
max_wal_senders = 10
wal_keep_segments = 64

# Primary server connection settings
primary_conninfo = 'host=master_ip port=5433 user=replicator password=replicator_password'
